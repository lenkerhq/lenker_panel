openapi: 3.0.3
info:
  title: Lenker Panel API
  version: 0.1.0-draft
  description: Draft OpenAPI contract for the currently implemented Lenker panel-api MVP v0.1 backend surface.
servers:
  - url: http://localhost:8080
    description: Local development
tags:
  - name: Health
  - name: Auth
  - name: Users
  - name: Plans
  - name: Subscriptions
  - name: Devices
  - name: Traffic
  - name: Nodes
  - name: Profiles
security:
  - bearerAuth: []
paths:
  /healthz:
    get:
      tags: [Health]
      summary: Health check
      security: []
      responses:
        "200":
          description: Service is running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthEnvelope"
  /api/v1/auth/admin/login:
    post:
      tags: [Auth]
      summary: Authenticate a panel admin
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminLoginRequest"
      responses:
        "200":
          description: Admin authenticated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminLoginEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "403":
          $ref: "#/components/responses/InactiveAdmin"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/users:
    get:
      tags: [Users]
      summary: List users
      responses:
        "200":
          description: Users list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Users]
      summary: Create a user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: User created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/users/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Users]
      summary: Get a user
      responses:
        "200":
          description: User details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Users]
      summary: Update a user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: User updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/users/{id}/suspend:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Users]
      summary: Suspend a user
      responses:
        "200":
          description: User suspended.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/users/{id}/activate:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Users]
      summary: Activate a user
      responses:
        "200":
          description: User activated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/plans:
    get:
      tags: [Plans]
      summary: List plans
      responses:
        "200":
          description: Plans list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Plans]
      summary: Create a plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePlanRequest"
      responses:
        "201":
          description: Plan created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/plans/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Plans]
      summary: Get a plan
      responses:
        "200":
          description: Plan details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Plans]
      summary: Update a plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePlanRequest"
      responses:
        "200":
          description: Plan updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/plans/{id}/archive:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Plans]
      summary: Archive a plan
      responses:
        "200":
          description: Plan archived.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions:
    get:
      tags: [Subscriptions]
      summary: List subscriptions
      responses:
        "200":
          description: Subscriptions list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Subscriptions]
      summary: Create a subscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSubscriptionRequest"
      responses:
        "201":
          description: Subscription created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Subscriptions]
      summary: Get a subscription
      responses:
        "200":
          description: Subscription details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Subscriptions]
      summary: Update a subscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSubscriptionRequest"
      responses:
        "200":
          description: Subscription updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/access:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Subscriptions]
      summary: Get provider-side subscription access export
      responses:
        "200":
          description: Subscription access export for the single MVP VLESS Reality path.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAccessEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Subscription access export is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/access-token:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Subscriptions]
      summary: Get subscription access token lifecycle status
      description: Returns provider-side lifecycle metadata for the current subscription access token without returning plaintext token material.
      responses:
        "200":
          description: Subscription access token lifecycle status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAccessTokenStatusEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Subscriptions]
      summary: Issue a subscription access token
      description: Issues the one active subscription access token for this subscription. Any previous active token for the subscription is revoked before the new plaintext token is returned once.
      responses:
        "201":
          description: Plaintext subscription access token created. The database stores only a token hash.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAccessTokenEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Subscription access token cannot be created for this subscription state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Subscriptions]
      summary: Revoke the active subscription access token
      description: Revokes the current active subscription access token without returning token material. The operation is idempotent; never-issued subscriptions remain never_issued and already-revoked tokens remain revoked.
      responses:
        "200":
          description: Active subscription access token revoked or already absent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAccessTokenStatusEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/access-token/rotate:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Subscriptions]
      summary: Rotate the active subscription access token
      description: Revokes the previous active token and returns a new plaintext token once. If no token exists yet, rotation behaves as a fresh issue.
      responses:
        "201":
          description: Plaintext replacement subscription access token created. The database stores only a token hash.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionAccessTokenEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Subscription access token cannot be rotated for this subscription state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/handoff-invite:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Subscriptions]
      summary: Get subscription handoff invite lifecycle status
      description: Returns provider-side lifecycle metadata for the latest client handoff invite without returning plaintext invite token material.
      responses:
        "200":
          description: Subscription handoff invite lifecycle status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionHandoffInviteStatusEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Subscriptions]
      summary: Issue a client handoff invite
      description: Issues a short-lived one-time handoff invite for the active subscription. Any previous active invite for the subscription is revoked before the new plaintext invite token is returned once.
      responses:
        "201":
          description: Plaintext handoff invite token created. The database stores only a token hash.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionHandoffInviteEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Handoff invite cannot be created for this subscription state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Subscriptions]
      summary: Revoke the active client handoff invite
      description: Revokes the current active handoff invite without returning token material. Already claimed, expired, revoked, or never-issued invites remain non-claimable.
      responses:
        "200":
          description: Active handoff invite revoked or already absent.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionHandoffInviteStatusEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/renew:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Subscriptions]
      summary: Manually renew a subscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenewSubscriptionRequest"
      responses:
        "200":
          description: Subscription renewed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/client/handoff/claim:
    post:
      tags: [Subscriptions]
      summary: Claim a client handoff invite
      description: Consumer-facing bootstrap boundary for the current single MVP path. The one-time handoff token is exchanged for a subscription access token plus the redacted access payload. It does not use admin session auth and does not create a full user account.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientHandoffClaimRequest"
      responses:
        "200":
          description: Handoff invite claimed and client access bootstrap payload returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientHandoffClaimEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Subscription access export is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/client/subscription-access:
    get:
      tags: [Subscriptions]
      summary: Read subscription access with a subscription access token
      description: Consumer-facing read boundary for the current single MVP VLESS Reality path. Uses Authorization Bearer subscription access tokens, not admin sessions.
      responses:
        "200":
          description: Redacted subscription access export.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientSubscriptionAccessEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Subscription access export is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes:
    get:
      tags: [Nodes]
      summary: List managed nodes
      responses:
        "200":
          description: Nodes list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/register:
    post:
      tags: [Nodes]
      summary: Register a node agent
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterNodeRequest"
      responses:
        "201":
          description: Node registered.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterNodeEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/InvalidBootstrapToken"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/bootstrap-token:
    post:
      tags: [Nodes]
      summary: Create a one-time node bootstrap token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNodeBootstrapTokenRequest"
      responses:
        "201":
          description: Bootstrap token created. The plaintext token is returned only once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeBootstrapTokenEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Nodes]
      summary: Get a managed node
      responses:
        "200":
          description: Node details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/config-revisions:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Nodes]
      summary: List node config revisions
      description: Returns signed deterministic subscription-aware config payload metadata. This foundation does not control the Xray runtime.
      responses:
        "200":
          description: Config revisions list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRevisionListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Nodes]
      summary: Create signed config revision metadata
      description: Creates metadata for a deterministic subscription-aware VLESS Reality Xray config skeleton payload. It does not restart processes or execute rollback.
      responses:
        "201":
          description: Config revision metadata created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRevisionEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/config-revisions/pending:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Nodes]
      summary: Fetch latest pending config revision metadata for a node
      description: Node-agent endpoint. Requires the node Bearer token and returns only the latest pending signed config payload metadata for the node in the path. The agent serializes local artifacts before reporting applied; this endpoint does not control Xray.
      responses:
        "200":
          description: Latest pending config revision metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRevisionEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Node is unknown, token does not match, node is disabled, or no pending revision exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/config-revisions/{revisionId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
        description: Config revision identifier.
    get:
      tags: [Nodes]
      summary: Get node config revision metadata
      responses:
        "200":
          description: Config revision metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRevisionEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/config-revisions/{revisionId}/rollback:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
        description: Applied config revision identifier to use as rollback source.
    post:
      tags: [Nodes]
      summary: Create rollback config revision metadata
      description: Admin endpoint. Creates a new pending revision from an applied target revision payload. The node-agent applies it through the normal polling path; panel-api does not mutate node files or restart Xray.
      responses:
        "201":
          description: Rollback config revision metadata created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRevisionEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/config-revisions/{revisionId}/report:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
        description: Config revision identifier.
    post:
      tags: [Nodes]
      summary: Report node config revision apply status
      description: Node-agent endpoint. Requires the node Bearer token and records applied or failed metadata status for a pending revision owned by the node. Applied means the node-agent verified hash/signature, passed the internal Xray compatibility validation gate, passed optional Xray binary dry-run validation when configured, and completed the local staged-to-active file switch. Failed reports keep node active_revision unchanged and may include compact reasons such as invalid_xray_config:missing_stream_settings or xray_dry_run_failed:invalid_config. This endpoint does not execute rollback or control Xray.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigRevisionReportRequest"
      responses:
        "200":
          description: Config revision status recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRevisionEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/drain:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Nodes]
      summary: Mark a node as draining
      responses:
        "200":
          description: Node marked as draining.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/undrain:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Nodes]
      summary: Return a node to active drain state
      responses:
        "200":
          description: Node undrained.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/disable:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Nodes]
      summary: Disable a node
      responses:
        "200":
          description: Node disabled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/enable:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Nodes]
      summary: Enable a disabled node
      responses:
        "200":
          description: Node enabled as unhealthy until the next heartbeat.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/heartbeat:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Nodes]
      summary: Record a node heartbeat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeHeartbeatRequest"
      responses:
        "200":
          description: Heartbeat accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeHeartbeatEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/devices:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Devices]
      summary: List devices for a subscription
      responses:
        "200":
          description: Devices list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/traffic:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Traffic]
      summary: Get subscription traffic usage
      description: Returns aggregated upload and download usage for a subscription over an optional RFC3339 period.
      parameters:
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Inclusive lower bound for recorded_at.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Inclusive upper bound for recorded_at.
      responses:
        "200":
          description: Subscription traffic usage.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficUsageEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/quota:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Traffic]
      summary: Get subscription traffic quota
      description: Returns current quota usage, optional limit, remaining bytes, and exceeded status.
      responses:
        "200":
          description: Subscription traffic quota.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficQuotaEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Traffic]
      summary: Set subscription traffic quota
      description: Creates or updates a subscription quota and records an audit log entry.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetTrafficQuotaRequest"
      responses:
        "200":
          description: Subscription traffic quota updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficQuotaEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/subscriptions/{id}/quota/reset:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Traffic]
      summary: Reset subscription traffic quota usage
      description: Sets bytes_used back to zero for a subscription quota and records an audit log entry.
      responses:
        "200":
          description: Subscription traffic quota reset.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficQuotaEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/devices/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Devices]
      summary: Get device detail
      responses:
        "200":
          description: Device detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Devices]
      summary: Revoke (delete) a device
      responses:
        "200":
          description: Device deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/devices/{id}/traffic:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Traffic]
      summary: Get device traffic usage
      description: Returns aggregated upload and download usage for a device over an optional RFC3339 period.
      parameters:
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Inclusive lower bound for recorded_at.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Inclusive upper bound for recorded_at.
      responses:
        "200":
          description: Device traffic usage.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficUsageEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/devices/{id}/deactivate:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Devices]
      summary: Soft-deactivate a device
      responses:
        "200":
          description: Device deactivated.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/traffic:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Traffic]
      summary: Get node traffic usage
      description: Returns aggregated upload and download usage for a node over an optional RFC3339 period.
      parameters:
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Inclusive lower bound for recorded_at.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Inclusive upper bound for recorded_at.
      responses:
        "200":
          description: Node traffic usage.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficUsageEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/traffic/report:
    post:
      tags: [Traffic]
      summary: Report traffic from a node agent
      description: |
        Accepts a batch of traffic records from a node agent. Auth via Bearer node token.
        Each entry maps a subscription (and optionally a device) to bytes uploaded and downloaded.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/TrafficReportItem"
              minItems: 1
      responses:
        "200":
          description: Traffic report accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrafficReportResultEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/warp:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [WARP]
      summary: Get WARP credentials for a node
      responses:
        "200":
          description: WARP credentials.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarpCredentialsEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [WARP]
      summary: Set WARP credentials for a node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarpCredentialsInput"
      responses:
        "200":
          description: WARP credentials set.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarpCredentialsEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [WARP]
      summary: Disable WARP for a node
      responses:
        "200":
          description: WARP disabled.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/warp/generate:
    post:
      tags: [WARP]
      summary: Generate a WireGuard keypair for WARP
      responses:
        "200":
          description: Generated keypair.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarpGenerateEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/settings:
    get:
      tags: [Settings]
      summary: List all global settings
      description: Returns all supported settings with current values or defaults.
      responses:
        "200":
          description: Global settings list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettingListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/settings/{key}:
    parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
    put:
      tags: [Settings]
      summary: Update a global setting
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSettingRequest"
      responses:
        "200":
          description: Setting updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettingEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/routing-rules/global:
    get:
      tags: [Routing]
      summary: List global routing rules
      responses:
        "200":
          description: Global routing rules.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRuleListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Routing]
      summary: Create a global routing rule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoutingRuleRequest"
      responses:
        "201":
          description: Routing rule created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRuleEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/routing-rules/global/{rule_id}:
    parameters:
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      tags: [Routing]
      summary: Update a global routing rule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoutingRuleRequest"
      responses:
        "200":
          description: Routing rule updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRuleEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Routing]
      summary: Delete a global routing rule
      responses:
        "200":
          description: Routing rule deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/routing-rules:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Routing]
      summary: List routing rules for a node (includes inherited globals)
      responses:
        "200":
          description: Node routing rules.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRuleListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Routing]
      summary: Create a per-node routing rule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoutingRuleRequest"
      responses:
        "201":
          description: Routing rule created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRuleEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/routing-rules/{rule_id}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: rule_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      tags: [Routing]
      summary: Update a per-node routing rule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoutingRuleRequest"
      responses:
        "200":
          description: Routing rule updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRuleEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Routing]
      summary: Delete a per-node routing rule
      responses:
        "200":
          description: Routing rule deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/nodes/{id}/routing-rules/reorder:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags: [Routing]
      summary: Reorder routing rules for a node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/RoutingRuleReorderEntry"
              minItems: 1
      responses:
        "200":
          description: Rules reordered.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/client/devices/register:
    post:
      tags: [Devices]
      summary: Register a client device
      description: Auth via Bearer subscription access token. Returns 403 if device_limit exceeded.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientDeviceRegisterRequest"
      responses:
        "200":
          description: Device registered or updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Device limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/client/devices/heartbeat:
    post:
      tags: [Devices]
      summary: Update device last_seen
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [device_fingerprint]
              properties:
                device_fingerprint:
                  type: string
      responses:
        "200":
          description: Heartbeat accepted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/client/devices/me:
    delete:
      tags: [Devices]
      summary: Delete current device
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [device_fingerprint]
              properties:
                device_fingerprint:
                  type: string
      responses:
        "200":
          description: Device deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/node-profiles:
    get:
      tags: [Profiles]
      summary: List node profiles
      responses:
        "200":
          description: Node profiles list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeProfileListEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Profiles]
      summary: Create a node profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNodeProfileRequest"
      responses:
        "201":
          description: Node profile created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeProfileEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/node-profiles/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags: [Profiles]
      summary: Get a node profile
      responses:
        "200":
          description: Node profile details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeProfileEnvelope"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags: [Profiles]
      summary: Update a node profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateNodeProfileRequest"
      responses:
        "200":
          description: Node profile updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeProfileEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Profiles]
      summary: Delete a node profile
      description: Only user-created profiles can be deleted. System profiles return 400.
      responses:
        "200":
          description: Node profile deleted.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/node-profiles/{id}/apply/{nodeId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: nodeId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Target node identifier.
    post:
      tags: [Profiles]
      summary: Apply a node profile to a node
      description: Copies routing rules from the profile config into the node's routing rules.
      responses:
        "200":
          description: Profile applied to node.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeProfileApplyEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: session-token
  parameters:
    ID:
      name: id
      in: path
      required: true
      schema:
        type: string
      description: Resource identifier.
  responses:
    BadRequest:
      description: Invalid request body or validation failure.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Unauthorized:
      description: Missing or invalid admin session token.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    NotFound:
      description: Resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    InvalidCredentials:
      description: Admin credentials are invalid.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    InactiveAdmin:
      description: Admin account is inactive.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    InternalError:
      description: Internal server error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    InvalidBootstrapToken:
      description: Node bootstrap token is invalid, expired, or already used.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    SuccessEnvelope:
      type: object
      required: [data]
      properties:
        data:
          description: Successful response payload.
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/APIError"
    APIError:
      type: object
      required: [code, message]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
    ErrorCode:
      type: string
      enum:
        - bad_request
        - unauthorized
        - not_found
        - validation_error
        - storage_error
        - internal_error
        - invalid_credentials
        - inactive_admin
        - invalid_bootstrap_token
        - expired_bootstrap_token
        - bootstrap_token_used
    HealthEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [ok]
    AdminLoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    AdminLoginEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: object
              required: [admin, session]
              properties:
                admin:
                  $ref: "#/components/schemas/Admin"
                session:
                  $ref: "#/components/schemas/AdminSession"
    Admin:
      type: object
      required: [id, email, status, two_factor_enabled, created_at, updated_at]
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        status:
          type: string
          enum: [active, suspended]
        two_factor_enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        last_login_at:
          type: string
          format: date-time
          nullable: true
    AdminSession:
      type: object
      required: [id, admin_id, token, expires_at, created_at]
      properties:
        id:
          type: string
        admin_id:
          type: string
        token:
          type: string
        expires_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    User:
      type: object
      required: [id, email, status, display_name]
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        status:
          type: string
          enum: [active, suspended, expired]
        display_name:
          type: string
    UserEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/User"
    UserListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/User"
    CreateUserRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
        display_name:
          type: string
    UpdateUserRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        display_name:
          type: string
    Plan:
      type: object
      required: [id, name, duration_days, device_limit, status]
      properties:
        id:
          type: string
        name:
          type: string
        duration_days:
          type: integer
          minimum: 1
        traffic_limit_bytes:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        device_limit:
          type: integer
          minimum: 1
        status:
          type: string
          enum: [active, archived]
    PlanEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/Plan"
    PlanListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/Plan"
    CreatePlanRequest:
      type: object
      required: [name, duration_days, device_limit]
      properties:
        name:
          type: string
        duration_days:
          type: integer
          minimum: 1
        traffic_limit_bytes:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        device_limit:
          type: integer
          minimum: 1
    UpdatePlanRequest:
      type: object
      properties:
        name:
          type: string
        duration_days:
          type: integer
          minimum: 1
        traffic_limit_bytes:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        clear_traffic_limit:
          type: boolean
        device_limit:
          type: integer
          minimum: 1
    Subscription:
      type: object
      required:
        - id
        - user_id
        - plan_id
        - status
        - starts_at
        - expires_at
        - traffic_used_bytes
        - device_limit
      properties:
        id:
          type: string
        user_id:
          type: string
        plan_id:
          type: string
        status:
          type: string
          enum: [active, expired, suspended]
        starts_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        traffic_limit_bytes:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        traffic_used_bytes:
          type: integer
          format: int64
          minimum: 0
        device_limit:
          type: integer
          minimum: 1
        preferred_region:
          type: string
          nullable: true
    SubscriptionEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/Subscription"
    SubscriptionListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/Subscription"
    SubscriptionAccess:
      type: object
      required:
        - export_kind
        - subscription_id
        - user_id
        - user_label
        - plan_id
        - plan_name
        - status
        - protocol
        - protocol_path
        - node
        - endpoint
        - client
        - display_name
        - uri
      properties:
        export_kind:
          type: string
          enum: [subscription_access.v1alpha1]
        subscription_id:
          type: string
        user_id:
          type: string
        user_label:
          type: string
        plan_id:
          type: string
        plan_name:
          type: string
        status:
          type: string
          enum: [active]
        protocol:
          type: string
          enum: [vless-reality-xtls-vision]
        protocol_path:
          type: string
          enum: [vless-reality-xtls-vision]
        node:
          type: object
          required: [id, name, region, country_code, hostname, status, drain_state, active_revision]
          properties:
            id:
              type: string
            name:
              type: string
            region:
              type: string
            country_code:
              type: string
            hostname:
              type: string
            status:
              type: string
              enum: [active]
            drain_state:
              type: string
              enum: [active]
            active_revision:
              type: integer
        endpoint:
          type: object
          required: [address, port, network, security, sni, public_key, short_id, fingerprint, spider_x]
          properties:
            address:
              type: string
            port:
              type: integer
              minimum: 1
            network:
              type: string
              enum: [tcp]
            security:
              type: string
              enum: [reality]
            sni:
              type: string
            public_key:
              type: string
            short_id:
              type: string
            fingerprint:
              type: string
            spider_x:
              type: string
        client:
          type: object
          required: [id, email, flow, level, plan_id]
          properties:
            id:
              type: string
            email:
              type: string
            flow:
              type: string
              enum: [xtls-rprx-vision]
            level:
              type: integer
            plan_id:
              type: string
        display_name:
          type: string
        uri:
          type: string
    SubscriptionAccessEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/SubscriptionAccess"
    SubscriptionAccessToken:
      type: object
      description: Plaintext provider handoff token returned only by issue or rotate responses.
      required: [subscription_id, access_token, expires_at, created_at]
      properties:
        subscription_id:
          type: string
          example: "sub_123"
        access_token:
          type: string
          description: Plaintext token returned only by the create-token response.
          example: "lnksa_example_plaintext_token"
        expires_at:
          type: string
          format: date-time
          example: "2026-06-16T12:00:00Z"
        created_at:
          type: string
          format: date-time
          example: "2026-05-17T12:00:00Z"
    SubscriptionAccessTokenEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/SubscriptionAccessToken"
    SubscriptionAccessTokenStatus:
      type: object
      description: Provider-side token lifecycle status. It never includes plaintext token material.
      required: [subscription_id, status, issued, generation]
      properties:
        subscription_id:
          type: string
          example: "sub_123"
        status:
          type: string
          enum: [never_issued, active, revoked]
          example: active
        issued:
          type: boolean
          example: true
        issued_at:
          type: string
          format: date-time
          nullable: true
          example: "2026-05-17T12:00:00Z"
        revoked_at:
          type: string
          format: date-time
          nullable: true
          example: null
        generation:
          type: integer
          example: 1
    SubscriptionAccessTokenStatusEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/SubscriptionAccessTokenStatus"
    SubscriptionHandoffInvite:
      type: object
      description: Plaintext one-time client handoff invite returned only by issue responses.
      required: [subscription_id, handoff_token, expires_at, created_at]
      properties:
        subscription_id:
          type: string
          example: "sub_123"
        handoff_token:
          type: string
          description: Plaintext handoff invite token returned only by the create-invite response.
          example: "lnkhi_example_plaintext_invite"
        expires_at:
          type: string
          format: date-time
          example: "2026-05-18T12:00:00Z"
        created_at:
          type: string
          format: date-time
          example: "2026-05-17T12:00:00Z"
    SubscriptionHandoffInviteEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/SubscriptionHandoffInvite"
    SubscriptionHandoffInviteStatus:
      type: object
      description: Provider-side handoff invite lifecycle status. It never includes plaintext invite token material.
      required: [subscription_id, status, issued, generation]
      properties:
        subscription_id:
          type: string
          example: "sub_123"
        status:
          type: string
          enum: [never_issued, active, claimed, revoked, expired]
          example: active
        issued:
          type: boolean
          example: true
        issued_at:
          type: string
          format: date-time
          nullable: true
          example: "2026-05-17T12:00:00Z"
        expires_at:
          type: string
          format: date-time
          nullable: true
          example: "2026-05-18T12:00:00Z"
        claimed_at:
          type: string
          format: date-time
          nullable: true
          example: null
        revoked_at:
          type: string
          format: date-time
          nullable: true
          example: null
        generation:
          type: integer
          example: 1
    SubscriptionHandoffInviteStatusEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/SubscriptionHandoffInviteStatus"
    ClientSubscriptionAccess:
      type: object
      required:
        - export_kind
        - subscription_id
        - status
        - protocol
        - protocol_path
        - plan_name
        - node
        - endpoint
        - client
        - display_name
        - uri
      properties:
        export_kind:
          type: string
          enum: [subscription_access.v1alpha1]
        subscription_id:
          type: string
        status:
          type: string
          enum: [active]
        protocol:
          type: string
          enum: [vless-reality-xtls-vision]
        protocol_path:
          type: string
          enum: [vless-reality-xtls-vision]
        plan_name:
          type: string
        node:
          $ref: "#/components/schemas/SubscriptionAccess/properties/node"
        endpoint:
          $ref: "#/components/schemas/SubscriptionAccess/properties/endpoint"
        client:
          type: object
          required: [id, email, flow, level]
          properties:
            id:
              type: string
            email:
              type: string
            flow:
              type: string
              enum: [xtls-rprx-vision]
            level:
              type: integer
        display_name:
          type: string
        uri:
          type: string
    ClientSubscriptionAccessEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ClientSubscriptionAccess"
    ClientHandoffClaimRequest:
      type: object
      required: [handoff_token]
      properties:
        handoff_token:
          type: string
          example: "lnkhi_example_plaintext_invite"
    ClientHandoffClaim:
      type: object
      required:
        - claim_kind
        - subscription_id
        - access_token
        - access_token_expires_at
        - claimed_at
        - access
      properties:
        claim_kind:
          type: string
          enum: [subscription_handoff_claim.v1alpha1]
        subscription_id:
          type: string
        access_token:
          type: string
          description: Plaintext subscription access token returned to the consumer after a successful one-time handoff claim.
          example: "lnksa_example_plaintext_token"
        access_token_expires_at:
          type: string
          format: date-time
        claimed_at:
          type: string
          format: date-time
        access:
          $ref: "#/components/schemas/ClientSubscriptionAccess"
    ClientHandoffClaimEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ClientHandoffClaim"
    CreateSubscriptionRequest:
      type: object
      required: [user_id, plan_id]
      properties:
        user_id:
          type: string
        plan_id:
          type: string
        preferred_region:
          type: string
          nullable: true
    UpdateSubscriptionRequest:
      type: object
      properties:
        status:
          type: string
          enum: [active, expired, suspended]
        traffic_limit_bytes:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        clear_traffic_limit:
          type: boolean
        device_limit:
          type: integer
          minimum: 1
        preferred_region:
          type: string
          nullable: true
        clear_preferred_region:
          type: boolean
    RenewSubscriptionRequest:
      type: object
      required: [extend_days]
      properties:
        extend_days:
          type: integer
          minimum: 1
    CreateNodeBootstrapTokenRequest:
      type: object
      properties:
        name:
          type: string
        region:
          type: string
        country_code:
          type: string
        hostname:
          type: string
        expires_in_minutes:
          type: integer
          minimum: 1
          maximum: 10080
          default: 30
    NodeBootstrapToken:
      type: object
      required: [id, node_id, bootstrap_token, expires_at]
      properties:
        id:
          type: string
        node_id:
          type: string
        bootstrap_token:
          type: string
          description: Plaintext one-time bootstrap token. It is returned only in this response.
        expires_at:
          type: string
          format: date-time
    NodeBootstrapTokenEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/NodeBootstrapToken"
    NodeSummary:
      type: object
      required: [id, name, region, status, drain_state, active_revision_id]
      properties:
        id:
          type: string
        name:
          type: string
        region:
          type: string
        status:
          type: string
          enum: [pending, active, unhealthy, drained, disabled]
        drain_state:
          type: string
          enum: [active, draining, drained]
        last_seen_at:
          type: string
          format: date-time
          nullable: true
        registered_at:
          type: string
          format: date-time
          nullable: true
        agent_version:
          type: string
        active_revision_id:
          type: integer
          minimum: 0
        runtime_mode:
          type: string
          description: Node-agent runtime supervision mode. MVP defaults to no-process or dry-run-only; no daemon is managed yet.
          enum: [no-process, dry-run-only, future-process-managed]
        runtime_process_mode:
          type: string
          description: Explicit node-agent local process runner gate. Default is disabled; local is an opt-in skeleton and does not ship daemon lifecycle management.
          enum: [disabled, local]
        runtime_process_state:
          type: string
          description: Latest process runner capability/preparation state. Disabled means no process control was attempted.
          enum: [disabled, ready, failed]
        runtime_desired_state:
          type: string
          description: Desired local runtime preparation state, currently validated-config-ready.
        runtime_state:
          type: string
          description: Latest local runtime preparation state reported by node-agent.
          enum: [not_prepared, active_config_ready, validation_failed, prepare_failed]
        last_dry_run_status:
          type: string
          enum: [not_configured, passed, failed]
        last_runtime_attempt_status:
          type: string
          enum: [skipped, ready, failed]
        last_runtime_prepared_revision:
          type: integer
          minimum: 0
        last_runtime_transition_at:
          type: string
          format: date-time
          nullable: true
        last_runtime_error:
          type: string
          description: Compact runtime preparation error, empty after successful no-process preparation.
        last_validation_status:
          type: string
          description: Latest node-agent config validation/apply result, empty when no validation has been reported yet.
          enum: ["", applied, failed]
        last_validation_error:
          type: string
          description: Compact latest validation/apply failure reason, empty after a successful apply or before first validation.
        last_validation_at:
          type: string
          format: date-time
          nullable: true
        last_applied_revision:
          type: integer
          minimum: 0
        active_config_path:
          type: string
          description: Node-local active config artifact path reported by node-agent.
    RuntimeEvent:
      type: object
      description: Compact node-agent runtime event retained as a bounded recent trail per node.
      properties:
        type:
          type: string
          enum: [apply_success, apply_failure, validation_failure, dry_run_failure, process_prepare_start_intent, runtime_event]
        status:
          type: string
          enum: ["", applied, failed, ready]
        revision_number:
          type: integer
          minimum: 0
        message:
          type: string
          maxLength: 240
        runtime_mode:
          type: string
          enum: [no-process, dry-run-only, future-process-managed]
        runtime_process_mode:
          type: string
          enum: [disabled, local]
        runtime_process_state:
          type: string
          enum: [disabled, ready, failed]
        at:
          type: string
          format: date-time
    Node:
      allOf:
        - $ref: "#/components/schemas/NodeSummary"
        - type: object
          properties:
            country_code:
              type: string
            hostname:
              type: string
            xray_version:
              type: string
            last_health_at:
              type: string
              format: date-time
              nullable: true
            runtime_events:
              type: array
              maxItems: 20
              items:
                $ref: "#/components/schemas/RuntimeEvent"
            updated_at:
              type: string
              format: date-time
    NodeEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/Node"
    NodeListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/NodeSummary"
    ConfigRevision:
      type: object
      required:
        - id
        - node_id
        - revision_number
        - status
        - bundle_hash
        - signature
        - signer
        - rollback_target_revision
        - created_at
      properties:
        id:
          type: string
        node_id:
          type: string
        revision_number:
          type: integer
          minimum: 1
        status:
          type: string
          enum: [pending, applied, failed, rolled_back]
        bundle_hash:
          type: string
          description: Stable hash of the deterministic config payload.
        signature:
          type: string
          description: Deterministic development signature for the MVP config delivery foundation.
        signer:
          type: string
        rollback_target_revision:
          type: integer
          minimum: 0
        bundle:
          type: object
          description: Deterministic subscription-aware VLESS Reality Xray-compatible skeleton payload with node metadata, operation_kind, optional rollback source metadata, subscription_inputs, access_entries, and rendered config object. It is not applied to a real Xray process by this foundation.
        created_at:
          type: string
          format: date-time
        applied_at:
          type: string
          format: date-time
          nullable: true
        failed_at:
          type: string
          format: date-time
          nullable: true
        rolled_back_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
          description: Compact node-agent failure reason. Xray compatibility validation failures use invalid_xray_config:<reason>; optional Xray binary dry-run failures use xray_dry_run_failed:<reason>.
          nullable: true
    ConfigRevisionReportRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [applied, failed]
        applied_at:
          type: string
          format: date-time
          nullable: true
        failed_at:
          type: string
          format: date-time
          nullable: true
        error_message:
          type: string
          nullable: true
        active_revision:
          type: integer
          minimum: 0
        runtime_mode:
          type: string
          enum: [no-process, dry-run-only, future-process-managed]
        runtime_process_mode:
          type: string
          enum: [disabled, local]
        runtime_process_state:
          type: string
          enum: [disabled, ready, failed]
        runtime_desired_state:
          type: string
        runtime_state:
          type: string
          enum: [not_prepared, active_config_ready, validation_failed, prepare_failed]
        last_dry_run_status:
          type: string
          enum: [not_configured, passed, failed]
        last_runtime_attempt_status:
          type: string
          enum: [skipped, ready, failed]
        last_runtime_prepared_revision:
          type: integer
          minimum: 0
        last_runtime_transition_at:
          type: string
          format: date-time
        last_runtime_error:
          type: string
        last_validation_status:
          type: string
          description: Latest node-agent validation/apply result.
          enum: [applied, failed]
        last_validation_error:
          type: string
          description: Compact latest validation/apply failure reason.
        last_validation_at:
          type: string
          format: date-time
        last_applied_revision:
          type: integer
          minimum: 0
        active_config_path:
          type: string
          description: Node-local active config artifact path.
        runtime_events:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/RuntimeEvent"
        sent_at:
          type: string
          format: date-time
          nullable: true
    ConfigRevisionEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ConfigRevision"
    ConfigRevisionListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/ConfigRevision"
    RegisterNodeRequest:
      type: object
      required: [bootstrap_token, agent_version]
      properties:
        node_id:
          type: string
        bootstrap_token:
          type: string
        agent_version:
          type: string
        hostname:
          type: string
    RegisterNodeResponse:
      type: object
      required: [node_id, node_token, status]
      properties:
        node_id:
          type: string
        node_token:
          type: string
        status:
          type: string
          enum: [active]
        drain_state:
          type: string
          enum: [active, draining, drained]
        registered_at:
          type: string
          format: date-time
          nullable: true
    RegisterNodeEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/RegisterNodeResponse"
    NodeHeartbeatRequest:
      type: object
      required: [agent_version, status]
      properties:
        node_id:
          type: string
        agent_version:
          type: string
        status:
          type: string
          enum: [pending, active, unhealthy, drained, disabled]
        active_revision:
          type: integer
          minimum: 0
        runtime_mode:
          type: string
          enum: [no-process, dry-run-only, future-process-managed]
        runtime_process_mode:
          type: string
          enum: [disabled, local]
        runtime_process_state:
          type: string
          enum: [disabled, ready, failed]
        runtime_desired_state:
          type: string
        runtime_state:
          type: string
          enum: [not_prepared, active_config_ready, validation_failed, prepare_failed]
        last_dry_run_status:
          type: string
          enum: [not_configured, passed, failed]
        last_runtime_attempt_status:
          type: string
          enum: [skipped, ready, failed]
        last_runtime_prepared_revision:
          type: integer
          minimum: 0
        last_runtime_transition_at:
          type: string
          format: date-time
        last_runtime_error:
          type: string
        last_validation_status:
          type: string
          description: Latest node-agent validation/apply result.
          enum: [applied, failed]
        last_validation_error:
          type: string
          description: Compact latest validation/apply failure reason.
        last_validation_at:
          type: string
          format: date-time
        last_applied_revision:
          type: integer
          minimum: 0
        active_config_path:
          type: string
          description: Node-local active config artifact path.
        runtime_events:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/RuntimeEvent"
        sent_at:
          type: string
          format: date-time
    NodeHeartbeatResponse:
      type: object
      required: [node_id, status, drain_state, active_revision]
      properties:
        node_id:
          type: string
        status:
          type: string
          enum: [pending, active, unhealthy, drained, disabled]
        drain_state:
          type: string
          enum: [active, draining, drained]
        active_revision:
          type: integer
          minimum: 0
        runtime_mode:
          type: string
          enum: [no-process, dry-run-only, future-process-managed]
        runtime_process_mode:
          type: string
          enum: [disabled, local]
        runtime_process_state:
          type: string
          enum: [disabled, ready, failed]
        runtime_desired_state:
          type: string
        runtime_state:
          type: string
          enum: [not_prepared, active_config_ready, validation_failed, prepare_failed]
        last_dry_run_status:
          type: string
          enum: [not_configured, passed, failed]
        last_runtime_attempt_status:
          type: string
          enum: [skipped, ready, failed]
        last_runtime_prepared_revision:
          type: integer
          minimum: 0
        last_runtime_transition_at:
          type: string
          format: date-time
          nullable: true
        last_runtime_error:
          type: string
        last_validation_status:
          type: string
          enum: ["", applied, failed]
        last_validation_error:
          type: string
        last_validation_at:
          type: string
          format: date-time
          nullable: true
        last_applied_revision:
          type: integer
          minimum: 0
        active_config_path:
          type: string
        runtime_events:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/RuntimeEvent"
        last_seen_at:
          type: string
          format: date-time
          nullable: true
    NodeHeartbeatEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/NodeHeartbeatResponse"
    Device:
      type: object
      required: [id, subscription_id, device_fingerprint, is_active, first_seen_at, last_seen_at, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        subscription_id:
          type: string
          format: uuid
        device_fingerprint:
          type: string
        device_name:
          type: string
          nullable: true
        platform:
          type: string
          nullable: true
          enum: [ios, android, windows, macos, linux]
        app_version:
          type: string
          nullable: true
        first_seen_at:
          type: string
          format: date-time
        last_seen_at:
          type: string
          format: date-time
        last_ip:
          type: string
          nullable: true
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DeviceEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/Device"
    DeviceListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/Device"
    TrafficLog:
      type: object
      required: [id, subscription_id, node_id, bytes_up, bytes_down, bytes_total, recorded_at, created_at]
      properties:
        id:
          type: string
          format: uuid
        subscription_id:
          type: string
          format: uuid
        device_id:
          type: string
          format: uuid
          nullable: true
        node_id:
          type: string
          format: uuid
        bytes_up:
          type: integer
          format: int64
          minimum: 0
        bytes_down:
          type: integer
          format: int64
          minimum: 0
        bytes_total:
          type: integer
          format: int64
          minimum: 0
        recorded_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    TrafficQuota:
      type: object
      required: [id, subscription_id, bytes_used, exceeded, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        subscription_id:
          type: string
          format: uuid
        bytes_limit:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        bytes_used:
          type: integer
          format: int64
          minimum: 0
        bytes_remaining:
          type: integer
          format: int64
          minimum: 0
          nullable: true
        exceeded:
          type: boolean
        reset_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TrafficUsage:
      type: object
      required: [resource_type, resource_id, bytes_up, bytes_down, bytes_total]
      properties:
        resource_type:
          type: string
          enum: [subscription, device, node]
        resource_id:
          type: string
          format: uuid
        bytes_up:
          type: integer
          format: int64
          minimum: 0
        bytes_down:
          type: integer
          format: int64
          minimum: 0
        bytes_total:
          type: integer
          format: int64
          minimum: 0
        from:
          type: string
          format: date-time
          nullable: true
        to:
          type: string
          format: date-time
          nullable: true
    TrafficQuotaEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/TrafficQuota"
    TrafficUsageEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/TrafficUsage"
    SetTrafficQuotaRequest:
      type: object
      properties:
        bytes_limit:
          type: integer
          format: int64
          minimum: 1
          nullable: true
          description: Null means unlimited traffic.
        bytes_used:
          type: integer
          format: int64
          minimum: 0
          description: Optional override for current used bytes. Omit to preserve current usage.
        reset_at:
          type: string
          format: date-time
          nullable: true
    TrafficReportItem:
      type: object
      required: [subscription_id, bytes_up, bytes_down]
      properties:
        subscription_id:
          type: string
          format: uuid
        device_id:
          type: string
          format: uuid
          nullable: true
        bytes_up:
          type: integer
          format: int64
          minimum: 0
        bytes_down:
          type: integer
          format: int64
          minimum: 0
    TrafficReportResult:
      type: object
      required: [node_id, accepted, bytes_up, bytes_down, bytes_total, reported_at]
      properties:
        node_id:
          type: string
          format: uuid
        accepted:
          type: integer
          minimum: 0
        bytes_up:
          type: integer
          format: int64
          minimum: 0
        bytes_down:
          type: integer
          format: int64
          minimum: 0
        bytes_total:
          type: integer
          format: int64
          minimum: 0
        reported_at:
          type: string
          format: date-time
    TrafficReportResultEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/TrafficReportResult"
    WarpCredentials:
      type: object
      required: [node_id, public_key, address, endpoint, enabled, created_at]
      properties:
        node_id:
          type: string
          format: uuid
        public_key:
          type: string
        address:
          type: string
        endpoint:
          type: string
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
    WarpCredentialsInput:
      type: object
      required: [private_key, public_key, address]
      properties:
        private_key:
          type: string
        public_key:
          type: string
        address:
          type: string
        endpoint:
          type: string
    WarpGenerateResult:
      type: object
      required: [private_key, public_key]
      properties:
        private_key:
          type: string
        public_key:
          type: string
    WarpCredentialsEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/WarpCredentials"
    WarpGenerateEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/WarpGenerateResult"
    GlobalSetting:
      type: object
      required: [key, value, updated_at]
      properties:
        key:
          type: string
        value: {}
        description:
          type: string
          nullable: true
        updated_by:
          type: string
          format: uuid
          nullable: true
        updated_at:
          type: string
          format: date-time
    SettingEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/GlobalSetting"
    SettingListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/GlobalSetting"
    UpdateSettingRequest:
      type: object
      required: [value]
      properties:
        value: {}
    RoutingRule:
      type: object
      required: [id, rule_type, target, action, priority, enabled, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        node_id:
          type: string
          format: uuid
          nullable: true
        rule_type:
          type: string
          enum: [geosite, geoip, domain, ip, port, protocol]
        target:
          type: string
        action:
          type: string
          enum: [block, proxy, direct, warp]
        outbound_tag:
          type: string
          nullable: true
        priority:
          type: integer
        enabled:
          type: boolean
        description:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    RoutingRuleEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/RoutingRule"
    RoutingRuleListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/RoutingRule"
    CreateRoutingRuleRequest:
      type: object
      required: [rule_type, target, action]
      properties:
        rule_type:
          type: string
          enum: [geosite, geoip, domain, ip, port, protocol]
        target:
          type: string
        action:
          type: string
          enum: [block, proxy, direct, warp]
        outbound_tag:
          type: string
        priority:
          type: integer
          default: 100
        enabled:
          type: boolean
          default: true
        description:
          type: string
    UpdateRoutingRuleRequest:
      type: object
      properties:
        rule_type:
          type: string
          enum: [geosite, geoip, domain, ip, port, protocol]
        target:
          type: string
        action:
          type: string
          enum: [block, proxy, direct, warp]
        outbound_tag:
          type: string
        priority:
          type: integer
        enabled:
          type: boolean
        description:
          type: string
    RoutingRuleReorderEntry:
      type: object
      required: [id, priority]
      properties:
        id:
          type: string
          format: uuid
        priority:
          type: integer
          minimum: 1
    ClientDeviceRegisterRequest:
      type: object
      required: [device_fingerprint]
      properties:
        device_fingerprint:
          type: string
          description: Hashed unique device identifier.
        device_name:
          type: string
          description: User-provided device name.
        platform:
          type: string
          enum: [ios, android, windows, macos, linux]
        app_version:
          type: string
    NodeProfile:
      type: object
      required: [id, name, is_system, config, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          nullable: true
        is_system:
          type: boolean
        config:
          type: object
          properties:
            routing_rules:
              type: array
              items:
                type: object
                properties:
                  rule_type:
                    type: string
                    enum: [geosite, geoip, domain, ip, port, protocol]
                  target:
                    type: string
                  action:
                    type: string
                    enum: [block, proxy, direct, warp]
                  outbound_tag:
                    type: string
                    nullable: true
                  priority:
                    type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    NodeProfileEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/NodeProfile"
    NodeProfileListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/NodeProfile"
    NodeProfileApplyEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: object
              required: [profile_id, node_id]
              properties:
                profile_id:
                  type: string
                  format: uuid
                node_id:
                  type: string
                  format: uuid
    CreateNodeProfileRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string
        config:
          type: object
    UpdateNodeProfileRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        config:
          type: object
